class Atom:
    def __init__(self, x, y, radius, dx, dy, outer_radius):
        self.x = x
        self.y = y
        self.radius = radius
        self.dx = dx
        self.dy = dy
        self.outer_radius = outer_radius

    